// Reset password page using the form component
import React from 'react';
import ResetPasswordForm from './reset-password-form';

const ResetPasswordPage = () => {
    return (
        <div>
            <h1>Reset Password</h1>
            <ResetPasswordForm />
        </div>
    );
};

export default ResetPasswordPage;
